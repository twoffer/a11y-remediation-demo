# a11y-remediation-demo
Before/after demos of common WCAG 2.x accessibility fixes on synthetic sample pages. Vanilla HTML/CSS/JS with no build step. Each fix is checked with axe, a keyboard-only pass, and NVDA.
